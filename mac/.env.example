# TTS (Text-to-Speech) Provider Configuration
# Options: vibevoice (default), qwen
#
# - vibevoice: Uses MLX-optimized VibeVoice Realtime (0.5B model)
#   Microsoft's expressive TTS model (DEFAULT)
#   Falls back to Qwen → macOS 'say' if model fails
#
# - qwen: Uses MLX-optimized Qwen3-TTS (0.6B model)
#   Fast, lightweight, good quality for general use
#   Falls back to macOS 'say' if model not available
#
TTS_PROVIDER=vibevoice

# Optional: Custom model paths
# Uncomment and set to use custom model locations
# TTS_MODEL_PATH=/path/to/custom/model
